#include <SDL.h>
#include <iostream>
//the error I made was not putting the parameters in the main function because I guess SDL is written in C and not C++
int main(int argc, char* argv[]){
	std::cout<<"it works";
	return 0;
}